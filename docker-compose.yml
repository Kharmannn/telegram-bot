services:
  expense-bot:
    build: .
    container_name: expense-bot
    restart: unless-stopped
    volumes:
      - ./credentials:/credentials:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
